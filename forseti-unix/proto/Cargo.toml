# Thin-deps invariant: this crate backs the NSS `.so`; keep deps tiny (no tokio/reqwest/rusqlite).
[package]
name = "forseti-unix-proto"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
